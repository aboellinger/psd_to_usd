# PSD to USD
PSD (Photoshop Document) converter to USD (Universal Scene Description)

## Features
* Layers (Basic)
* Output to USD, USDA, USDC, USDZ

## TODO
* opacityThreshold for text


## Development

You can clone this repo and install it into your development virtualenv with:
```bash
pip install -e .
```
